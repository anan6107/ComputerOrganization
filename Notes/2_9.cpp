IA.Tutorial.Model.zip

Couple definitions on the slides, but you should already know them

RISC
  Load/Store 
    sign extend
    - lb rd, offset(rs1)
    - lh rd, offset(rs1)
    - lw rd, offset(rs1)
    unsigned
    - lbu rd, offset(rs1)
    - lhu rd, offset(rs1)
    - lwu rd, offset(rs1)
    store rightmost
    - sb rs2, offset (rs1)
    - su rs2, offset (rs1)
    - sw rs2, offset (rs1)
